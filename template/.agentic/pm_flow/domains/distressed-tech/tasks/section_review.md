## Domain task delta

In the acceptance matrix, re-fetch one load-bearing fact from its cited source
and counter-search the claim most likely to be stale or wrong. Reject a fact
without a carrying quote, a missing retrieval date, an unresolved entity
collision, or a primary-source claim supported only by an aggregator when the
primary document is reachable.
