## Domain task delta

For each assigned fact, return the source URL, retrieval date, and the verbatim
quote that carries it. Use `.agentic/pm_flow/fetch.sh` for external reads. Under
Validation include the counter-search against the central claim. `DELIVERED`
requires every requested fact to be sourced or explicitly recorded as
unobtainable with its mechanism.
