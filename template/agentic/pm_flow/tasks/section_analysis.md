---

# Task: assess section `{{SECTION_KEY}}` now

The owner asked for this section's position, outside the development loop. This
is an assessment, not an assignment.

Read:

{{CONTEXT_FILES}}

The section is at cycle {{CYCLE}}. If a focusing question is among the files
above, answer it first and explicitly; everything below still applies.

## This call opens nothing

- Do not write an assignment, and do not name a next developer task as though
  one were being dispatched. Say what you would do next and why; the owner
  decides whether it happens.
- Do not create, advance or edit a cycle. No cycle directory, no `assignment.md`,
  no `decision.txt`, no verdict token. Nothing parses your answer, so a token is
  noise that reads as a dispatch that never happened.
- Do not answer `ASSIGN`, `COMPLETE` or `BLOCKED_EXTERNAL`. Those are scope
  verdicts and this is not a scope call. `COMPLETE` here would claim a section
  closed that nothing reviewed.
- You may write `state.md` if what you found corrects it, and you may commit
  that. Change nothing else.

## What the answer has to settle

- Where the section stands against its own acceptance criteria, criterion by
  criterion. Each one is met, not met, or unknown, and each verdict names the
  observation behind it. A criterion you have not tested is unknown, and
  unknown is an answer.
- What is blocking it, if anything, and whether the blocker is difficulty, an
  external dependency, or a decision nobody has taken.
- What you would do next and why that before anything else.
- What you cannot settle yourself: the questions whose answers are above your
  scope, need the owner, or need another section.

Run the acceptance check if the brief names one, and paste what it printed. A
criterion is not met because the last review said so.

## Respond with these sections only, each as a Markdown heading

1. Where the section stands
2. What is blocking it
3. What I would do next and why
4. What I cannot settle myself

No Decision section. This call has no verdict.
