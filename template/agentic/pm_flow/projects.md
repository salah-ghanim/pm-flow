# Projects

Use this file to track the available project workspaces under `agentic/pm_flow/`.

Selection rules:

- `pm_flow.sh` defaults to the repo basename when that project directory exists.
- Override with `--project <name>` or `PM_FLOW_PROJECT=<name>` when needed.

Installed projects:
